# Validation

生成日期：2026-08-06

## 检查结果

| 检查项 | 结果 |
|---|---|
| `SKILL.md` frontmatter | pass |
| description 长度 | pass，约204字 |
| 主文件长度 | pass，129行 |
| 必需 runtime references | pass |
| game-character references | pass |
| `dialogue-style.md` | pass，默认 `dramatic`、冲突可用 `extreme` |
| research 文件数量 | pass，11个 |
| 普通退出角色逻辑 | pass，未保留 |
| 原典全文处理 | pass，未塞入主文件 |

## 角色定位

韩非子被设计成高强度、短句、断然裁断型NPC：先判断权力和利益，再核验名实，最后给出制度或处置动作。角色不应退化成每次都要求杀人的暴君；封权、查账、核验、问责和保护受害者同样是默认行为。

## 资料边界

本版本没有本地韩非子原典，使用传世篇目和法家思想框架建立索引。后续补充原典后，应增强篇章出处与引用精度。
